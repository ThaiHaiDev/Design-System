Tabs switch between sibling views within the same page section (e.g. Overview / Activity / Settings).

```jsx
<Tabs tabs={[{id:"overview", label:"Overview"}, {id:"activity", label:"Activity"}]} activeId={tab} onSelect={setTab} />
```
