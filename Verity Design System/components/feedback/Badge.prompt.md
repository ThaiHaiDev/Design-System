Badge is a small status/label pill — use status-* variants for state (success/error/warning) and accent for counts or "new" markers.

```jsx
<Badge variant="success">Active</Badge>
<Badge variant="error">Failed</Badge>
```
