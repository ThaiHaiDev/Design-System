Toast is a transient bottom-right notification. Success/info auto-dismiss after 5s; errors persist until closed.

```jsx
<Toast variant="success" title="Saved" description="Your changes were saved." onClose={dismiss} />
```
